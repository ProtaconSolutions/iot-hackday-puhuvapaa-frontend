/**
 * Copy this file to 'config.json' and define all settings what your environment is using
 *
 * Also remember remove this comment block too :D
 */
{
  "config": {
    "backendUrl": "localhost"
  }
}